spring:
  jackson:
    serialization.indent_output: true
    serialization.write_dates_as_timestamps: false
    timeZone: GMT+8

  mvc:
    favicon:
      enabled: false

server:
  port: 8080
  session:
    cookie:
      http-only: true


